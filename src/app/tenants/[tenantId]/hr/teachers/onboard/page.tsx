/**
 * Teacher management under HR module.
 * Re-exports the canonical teacher onboard page — same component,
 * accessed via the HR tab strip instead of the old top-level nav item.
 */
export { default } from '@/app/tenants/[tenantId]/teachers/onboard/page';
