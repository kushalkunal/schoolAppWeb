package in.schoolapp.student.entity;

/**
 * Document categories that determine default retention + where the UI groups the file.
 */
public enum StudentDocumentType {
    ADMISSION_FORM,
    BIRTH_CERT,
    TC,
    PHOTO,
    MEDICAL,
    OTHER
}
